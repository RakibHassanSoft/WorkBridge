// The mentor role was merged into the coordinator/moderator role.
// This stub remains only so any stale import still compiles; it renders nothing.
export default function MentorWorkspace() {
  return null;
}
